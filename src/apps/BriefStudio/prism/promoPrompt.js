// BriefStudio · Prism — prism/promoPrompt.js
// "오늘의 상품 스페셜" 수정요청서 → 플러그인 구조화 데이터 추출 프롬프트.
// 디자인 구조(현재 오상스 페이지 구성)를 모델에 명시해, 비정형 요청서를 정해진 슬롯에 매핑한다.

export function promoExtractionPrompt(note = '') {
  return `당신은 NC "오늘의 상품 스페셜" 월간 프로모션 페이지의 수정요청서를 읽고, 디자인 슬롯 구조에 맞춰 데이터를 전사(추출)하는 전문가입니다.
요청서(이미지/PDF/텍스트)에서 값을 **원문 그대로** 뽑아 아래 구조로 출력하세요. 추정/창작 금지 — 없는 값은 빈 문자열로 두고 uncertain 에 적습니다.

[페이지 디자인 구조 — 이 슬롯들에 매핑]
1) meta
   - game: 게임명, month: 대상 월
   - title: **실제 페이지/배너에 노출되는 타이틀만**(예 "6월 오늘의 상품 스페셜"). 문서 표지 제목의 "[프로모션]" 같은 대괄호 접두어·문서 양식 표기는 빼고 순수 페이지 타이틀로.
   - salePeriod: 상품 판매 기간 원문, rewardPeriod: 보상 수령 기간 원문
2) boxes (스페셜 상품 4칸) — 카드마다 윗줄(top)+아랫줄(name). 보통 top="오늘의 선물상자", name=실제 상품명(예 "각성 사도진","사선 태극","팔성 수호").
   "디자인 수정" 표 / 상품 카드 영역의 상품명에서 그대로 가져온다. top 라벨이 없는 상품(예 "영롱한 월석 금화 상자")은 top 비우고 name 만.
   요청서 상품 **좌→우 순서 그대로** 1~4번에 넣는다. **박스 상품명을 slotIcons 에 넣지 말 것.**
3) slotIcons — 상품 1~4번 카드에 삽입될 **대표 아이콘 아이템명** (boxes 순서와 1:1).
   ★ 입력에 "[붉은색 대표 아이콘 (상자→아이템)]" 목록이 있으면, **그 목록의 item 을 boxes 순서에 맞춰 그대로 slotIcons 로 쓴다(최우선).**
   ★★ 한 상자에 빨강 항목이 **여러 개면 전부**를 ` + ` 로 이어붙여 그 상자의 slotIcons 한 항목으로 넣는다 (첫 번째만 넣지 말 것). **구분자는 반드시 ` + ` — <br>·줄바꿈·쉼표 금지** (플러그인이 ` + ` 로만 분리해 여러 아이콘을 한 장으로 합성한다).
       예: "홍문수 결정 400개 + 수호수 결정 400개 + 광혼석 결정 400개" — 플러그인이 이걸 분리해 한 장으로 합성한다.
   그 목록이 없을 때만: "상세 구성/삽입아이콘" 표의 **붉은색([빨강]) 항목**을 대표 아이콘으로(여러 개면 동일하게 ` + ` 로 전부).
   (예 "진무강 200개","프리미엄 삼선 수호 보석 선택 상자","태극 오선 보석 선택함")
   ※ 박스 제목/상품명이 아니라 그 상자 구성품 중 하나의 아이템 이름이다. **오직 빨간색 글자 항목만** — 검정/회색 항목은 대표 아이콘이 아니다. 붉은색 근거가 없으면 빈 문자열(추정 금지).
4) rollovers — 각 상품 카드(1~4)에 마우스 올리면 뜨는 **상세 구성** 목록. card="1".."4", items=[{name, count}]. count 예 "5개","3,000개".
   "각 아이템의 상세 구성" 표의 해당 상자 열에 적힌 항목을 **하나도 빠짐없이 전부** 넣는다(4줄이면 4줄, 5줄이면 5줄).
   임의 요약·생략·축소 금지. 붉은색(대표 아이콘) 항목도 이 목록에 그대로 포함한다.
5) milestones (보너스 5단계) — 누적 보너스 단계 5개. 각 항목: name, threshold, magnify.
   - threshold = 그 카드 상단 "N 누적 시"의 숫자 문구("5,000 누적 시"→"5,000"). 카드마다 다르며 보통 좌→우 오름차순(예 5,000·10,000·20,000·30,000·40,000). 요청서/이미지에 적힌 값 그대로, 임의로 같은 값을 모든 카드에 넣지 말 것.
   - magnify = **돋보기(상세보기) 아이콘이 실제로 붙은 카드만** true. 보통 5개 중 일부(예 2개)만 true이고 나머지는 false. 모든 카드에 true 금지 — 돋보기 표시가 없는 카드는 반드시 false.
   예: 환영무기 교환권(threshold "5,000", magnify true) · 진화석 결정(10,000, false) · 의상 교환권(20,000, true) · 화신석(30,000, false) · 결정(40,000, false).
6) popupDays (전체상품 자세히 보기 팝업) — 일자별 표. date="6월 25일 (목)", rows=[{name, price}].
   price 는 콤마 포함 원문("4,000"), 무료/비매는 "0". 하루 최대 8행.
7) popupTexts (개별 팝업 타이틀·아이템명) — **돋보기(magnify=true) 보너스마다 상세 팝업이 1개씩** 있고, 그 팝업의 제목·아이템명을 슬롯키로 추출. key=슬롯키, value=문구. 슬롯키 규칙:
   - 무기제목 = 환영무기 상세 팝업 제목 (예 "판다 환영무기 상자")
   - 천체조각제목 = 천체조각 선택권 팝업 제목 (예 "천체조각 선택권")
   - 천체조각1명 / 천체조각2명 = 천체조각 선택 아이템 2개(보통 "OR"로 갈리는 좌/우), 개수까지 포함 (예 "고대의 태천명왕 천체 조각 60개" / "적패왕 천체 조각 60개")
   - 흑장미제목 = 흑장미 환영비급 팝업 제목 (예 "흑장미 환영비급")
   - 의상제목 = 판다 의상 세트 팝업 제목 (예 "판다와 함께 의상 세트")
   요청서/이미지에 적힌 표기 그대로(띄어쓰기·개수 포함) 옮기고, 임의 축약 금지. 줄바꿈은 value 안에 \\n. 해당 팝업이 이번 달에 없으면 그 키는 생략.
8) replacements (슬롯 없는 고정문구 치환) — 배너 제목/판매·수령 기간/팝업 공지 날짜 등 "이전→새".
   before=요청서가 말하는 기존 문구(없으면 직전 월 추정 대신 비워두고 uncertain 처리), after=새 문구.

[무시할 문서 양식 요소 — 절대 추출/슬롯에 넣지 말 것]
- 문서 표지·제목 페이지: "[프로모션] …" 같은 **문서 제목**, 게임/회사 로고(B&S 등), 문서 머리말·꼬리말, 페이지 번호.
- 작성 정보 표: **팀명 · 작성자명 · 내선번호 · 작성일 · 버전/개정 이력** 표.
- 이런 값은 meta.title 을 포함한 어떤 슬롯·필드에도 넣지 않는다. (요청서 "내용"이 아니라 문서 양식이다.)

[★★ 가장 중요 — "수정 희망 영역" 이미지는 '수정 전(옛)' 화면이다]
- 요청서는 보통 위쪽 "수정 희망 영역"(배너/화면 캡처 이미지, 번호 ①②③④ 표시) + 아래쪽 "반영 희망 내용"(변경 표·목록)으로 구성된다.
- **"수정 희망 영역" 이미지는 바꾸기 "전"의 옛 화면이다.** 거기 보이는 타이틀·상품명(예 "5월의 스페셜 상품", "태극 오선", "진무보패함 (A)/(B)", "수호 삼선" 등)은 **교체될 옛 값**이지 추출할 데이터가 아니다. **이미지(배너 캡처)에서 boxes·slotIcons·rollovers·title 을 절대 가져오지 말 것.**
- 추출할 **새 데이터는 오직 "반영 희망 내용"의 표/목록**에 있다:
  - "디자인 수정" 표(IMG 헤더 + 상품명 줄) → boxes (윗줄/상품명). **새 상품명은 여기서만** (예 "오늘의 선물상자 진무", "화려한 결정 금화상자", "오늘의 선물상자 수호 삼선", "오늘의 선물상자 태극 오선").
  - "상자 및 삽입아이콘"에서 **붉은색(빨강)** 으로 표시된 항목 → slotIcons (대표 아이콘).
  - "각 아이템의 상세 구성" 표(상자별 열) → rollovers (해당 열 전부).
  - "텍스트 수정  A → B"(예 "5월의 스페셜 상품 → 6월의 스페셜 상품") → meta.title=B + replacements{before:A, after:B}.
  - "일정 수정 / 판매 기간 A→B" → meta.salePeriod=B + replacements; "수령 기간 A→B" → meta.rewardPeriod=B + replacements.
  - "메인 리소스/배경 변경"은 이미지 교체라 텍스트 슬롯에 넣지 말고 uncertain 에 "메인 리소스 변경(이미지)"로 메모.
- **이미지와 "반영 희망 내용"이 다르면 항상 "반영 희망 내용"을 따른다.** 이미지의 옛 상품(태극 오선·진무보패함 등)을 결과(boxes 등)에 절대 넣지 말 것.
- "반영 희망 내용"에 **명시된 것만** 반영하고, 목록에 없는 본문/팝업 데이터는 추정해 채우지 않는다.

[필드 구분 — 절대 섞지 말 것]
- 한 상자는 서로 다른 3개 값을 갖는다: ① boxes[i].name = 카드 상품명, ② slotIcons[i] = 그 상자 대표 아이콘(붉은색 아이템), ③ rollovers(card=i+1).items = 상세 구성 전체.
- 박스 상품명을 아이콘 칸에, 아이콘을 롤오버에 잘못 넣지 말 것. 같은 i 의 세 값은 모두 다르다.
- 상품명·아이템명은 **이미지 원문 그대로** — 오탈자 교정·유사 단어 대체·창작 금지. 요청서에 없는 이름(예 직전 월 상품명)을 만들거나 끌어오지 말 것.
- boxes·slotIcons·rollovers·milestones·popupDays 는 요청서에 등장하는 **순서 그대로** 정렬.

[규칙]
- **모든 목록(상품 4칸·각 카드 롤오버·보너스 5단계·팝업 일자별 행)은 요청서에 있는 만큼 전부** 추출한다. 개수를 임의로 줄이지 않는다.
- 숫자/날짜/단위/괄호는 요청서 원문 유지. 임의 정규화 금지.
- boxes 와 slotIcons 는 개수·순서를 맞춘다(보통 4개).
- 판단이 애매하거나 요청서에 근거 없는 값은 uncertain 배열에 한 줄씩 사유와 함께.
- 출력은 JSON 하나만(코드블록·설명 금지).${note ? `\n\n[추가 지시 — 우선 적용]\n${note}` : ''}`;
}

// 1단계 전사 — 이미지/PDF에서 "반영 희망 내용" 섹션만 텍스트로 옮긴다(배너 이미지 무시).
// 이후 2단계 구조화 추출은 이 전사 텍스트만 사용(이미지 미전달)하여 옛 배너에 휘둘리지 않게 한다.
export function transcribePrompt() {
  return `이 수정요청서에서 **"반영 희망 내용"** 섹션의 모든 텍스트를 표·목록 구조를 살려 **빠짐없이 그대로 옮겨 적어라(전사)**.

[중요]
- 위쪽 **"수정 희망 영역"의 배너/화면 캡처 이미지(번호 ①②③④ 표시)는 완전히 무시**한다 — 그건 수정 "전" 옛 화면이라 전사하지 않는다.
- 아래쪽 "반영 희망 내용"의 변경 목록만 옮긴다: "텍스트 수정", "디자인 수정"(IMG 헤더 + 상품명 표), "상자 및 삽입아이콘", "각 아이템의 상세 구성"(상자별 열 표), "일정 수정" 등.
- 표는 **어느 상자(열)에 어떤 항목(행)이 있는지 알아볼 수 있게** 옮긴다(상자명을 머리로, 그 아래 항목들).
- 원문 그대로 — 오탈자 교정·요약·창작 금지.

[★ 빨간색 대표 아이콘 — 가장 중요, 색을 똑똑히 보고 판단]
- "상자 및 삽입아이콘 / 각 아이템의 상세 구성" 표에서, **글자색이 빨강(붉은) 계열로 인쇄된 항목**을 각 상자(열)별로 찾아라. 검정/흰색/회색 항목은 제외, **빨강으로 강조된 것만**.
- 이 빨강 항목이 그 상자의 "**대표 아이콘**"이다. 한 상자에 빨강이 여러 개면 모두 적되 **첫 번째가 대표**.
- 찾은 것을 두 곳에 남겨라: ① transcript 본문 해당 항목 끝에 [빨강] 표기, ② **redItems 배열**에 {box: 상자 상품명, item: 빨강 항목명}으로 명시(이 배열이 핵심).
- 빨강이 하나도 없으면 redItems 는 빈 배열. 색이 애매하면 가장 빨강에 가까운 항목을 넣는다.
- 출력은 {"transcript":"...전사...","redItems":[{"box":"...","item":"..."}]} JSON 하나만(코드블록·설명 금지).`;
}

// 2차(이후) 수정요청서 병합 — 1차 확정 데이터에 변경/추가분만 반영해 전체 데이터를 다시 출력.
export function promoMergePrompt(prevJson, note = '') {
  return `${promoExtractionPrompt(note)}

[★ 2차 수정요청서 병합 모드 — 가장 중요]
아래 "기존(직전 차수) 데이터"는 이미 확정된 값이다. 첨부/입력한 이번 차수 수정요청서에는 보통 **직전 대비 변경·추가된 부분만** 담겨 있다.
- 기존 데이터를 **기준(base)** 으로 삼아, 이번 요청서에서 바뀐/추가된 항목만 반영한 **전체 병합 결과**를 출력한다.
- 이번 요청서에 언급이 없는 항목은 **기존 값을 그대로 유지**한다(임의 삭제·초기화·재작성 금지).
- 이번 요청서가 "삭제/제외"라고 명시한 항목만 제거한다.
- popup(일자별 표)·boxes·milestones 등은 같은 위치/같은 키를 덮어쓰고, 새 항목은 알맞은 자리에 추가한다.
- 단, 이번 요청서가 상품 카드("디자인 수정"/상품 표)를 **새 상품들로 전면 교체**하면, boxes·slotIcons·rollovers 를 그 표의 순서·이름대로 **완전히 교체**한다(이전 차수의 상품명·아이콘·롤오버를 절대 남기지 않는다). 새 요청서에 있는 값만 사용한다.
- 반영한 변경/추가/삭제 내용을 **changes 배열**에 사람이 읽을 수 있게 한 줄씩 적는다(예: "상품3 가격 4,000→5,000", "보너스5 교체: 뇌천석 결정→…", "7/8 일자 추가").

[기존(직전 차수) 데이터]
${prevJson}`;
}
